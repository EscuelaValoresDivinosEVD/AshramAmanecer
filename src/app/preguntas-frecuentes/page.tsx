import ContentPage, { pageMetadata } from "@/components/site/ContentPage";

export const metadata = pageMetadata("/preguntas-frecuentes/");

export default function Page() {
  return <ContentPage path="/preguntas-frecuentes/" />;
}
