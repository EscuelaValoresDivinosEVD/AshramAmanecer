import ContentPage, { pageMetadata } from "@/components/site/ContentPage";

export const metadata = pageMetadata("/amenidades/");

export default function Page() {
  return <ContentPage path="/amenidades/" />;
}
