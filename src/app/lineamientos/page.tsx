import ContentPage, { pageMetadata } from "@/components/site/ContentPage";

export const metadata = pageMetadata("/lineamientos/");

export default function Page() {
  return <ContentPage path="/lineamientos/" />;
}
